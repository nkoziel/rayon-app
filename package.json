{
  "name": "rayon-app",
  "private": true,
  "version": "1.0.0",
  "description": "Manga library, progress tracking and AniList recommendations. Standalone PWA.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node tools/publish.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "structure": "node tools/check-refs.js",
    "verify": "node tools/verify.js && node tools/check-refs.js && vitest run"
  },
  "devDependencies": {
    "happy-dom": "^20.11.6",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0",
    "vitest": "^4.1.11"
  }
}
